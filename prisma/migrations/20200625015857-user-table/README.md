# Migration `20200625015857-user-table`

This migration has been generated by pedroksty at 6/25/2020, 1:58:57 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"alerta_vagas" boolean  NOT NULL DEFAULT false,"area" text  NOT NULL ,"cpf" integer  NOT NULL ,"created_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"data_nascimento" text  NOT NULL ,"email" text  NOT NULL ,"escolaridade" text  NOT NULL ,"genero" text  NOT NULL ,"id" SERIAL,"login" text  NOT NULL ,"name" text  NOT NULL ,"password" text  NOT NULL ,"sit_emprego" text  NOT NULL ,"telefone" integer  NOT NULL ,"updated_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Addres" (
"bairro" text  NOT NULL ,"cep" integer  NOT NULL ,"cidade" text  NOT NULL ,"id" SERIAL,"numero" integer  NOT NULL ,"rua" text  NOT NULL ,"uf" text  NOT NULL ,"user_id" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "User.cpf" ON "public"."User"("cpf")

CREATE UNIQUE INDEX "Addres_user_id" ON "public"."Addres"("user_id")

ALTER TABLE "public"."Addres" ADD FOREIGN KEY ("user_id")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200625012530-fix-addres-uf..20200625015857-user-table
--- datamodel.dml
+++ datamodel.dml
@@ -3,22 +3,22 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
   id                  Int    @default(autoincrement()) @id
   name                String
   email               String @unique
-  cpf                 Float @unique
+  cpf                 Int @unique
   area                String
   login               String
   password            String
   data_nascimento     String
   genero              String
-  telefone            Float
+  telefone            Int
   escolaridade        String
   alerta_vagas        Boolean @default(false)
   sit_emprego         String
   created_at          DateTime @default(now())
@@ -28,12 +28,12 @@
 model Addres {
   id      Int    @id @default(autoincrement())
   rua String
-  numero Float
+  numero Int
   bairro String
   cidade String
   uf String
-  cep Float
+  cep Int
   user    User   @relation(fields: [user_id], references: [id])
   user_id  Int    
 }
```



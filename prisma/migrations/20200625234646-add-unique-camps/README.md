# Migration `20200625234646-add-unique-camps`

This migration has been generated by pedroksty at 6/25/2020, 11:46:46 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."User" ADD COLUMN "image" text  NOT NULL ;

CREATE UNIQUE INDEX "Enterprise.email" ON "public"."Enterprise"("email")

CREATE UNIQUE INDEX "Enterprise.login" ON "public"."Enterprise"("login")

CREATE UNIQUE INDEX "Enterprise.cnpj" ON "public"."Enterprise"("cnpj")

CREATE UNIQUE INDEX "User.login" ON "public"."User"("login")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200625233214-enterprise-and-addres-enterprise..20200625234646-add-unique-camps
--- datamodel.dml
+++ datamodel.dml
@@ -3,19 +3,20 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
   id              Int      @default(autoincrement()) @id
   name            String
   email           String   @unique
   cpf             Float    @unique
   area            String
-  login           String
+  login           String   @unique
   password        String
+  image           String
   data_nascimento String
   genero          String
   telefone        Float
   escolaridade    String
@@ -40,12 +41,12 @@
 model Enterprise {
   id               Int              @id @default(autoincrement())
   name             String
-  email            String
-  login            String
+  email            String           @unique
+  login            String           @unique
   password         String
-  cnpj             Float
+  cnpj             Float            @unique
   telefone         Float
   addresInterprise AddresEnterprise
   image            String?
   created_at       DateTime         @default(now())
```



# Migration `20200625020814-fix-table`

This migration has been generated by pedroksty at 6/25/2020, 2:08:14 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"alerta_vagas" boolean  NOT NULL DEFAULT false,"area" text  NOT NULL ,"cpf" Decimal(65,30)  NOT NULL ,"created_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"data_nascimento" text  NOT NULL ,"email" text  NOT NULL ,"escolaridade" text  NOT NULL ,"genero" text  NOT NULL ,"id" SERIAL,"login" text  NOT NULL ,"name" text  NOT NULL ,"password" text  NOT NULL ,"sit_emprego" text  NOT NULL ,"telefone" Decimal(65,30)  NOT NULL ,"updated_at" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Addres" (
"bairro" text  NOT NULL ,"cep" Decimal(65,30)  NOT NULL ,"cidade" text  NOT NULL ,"id" SERIAL,"numero" Decimal(65,30)  NOT NULL ,"rua" text  NOT NULL ,"uf" text  NOT NULL ,"user_id" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "User.cpf" ON "public"."User"("cpf")

CREATE UNIQUE INDEX "Addres_user_id" ON "public"."Addres"("user_id")

ALTER TABLE "public"."Addres" ADD FOREIGN KEY ("user_id")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200625015857-user-table..20200625020814-fix-table
--- datamodel.dml
+++ datamodel.dml
@@ -3,22 +3,22 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model User {
   id                  Int    @default(autoincrement()) @id
   name                String
   email               String @unique
-  cpf                 Int @unique
+  cpf                 Float @unique
   area                String
   login               String
   password            String
   data_nascimento     String
   genero              String
-  telefone            Int
+  telefone            Float
   escolaridade        String
   alerta_vagas        Boolean @default(false)
   sit_emprego         String
   created_at          DateTime @default(now())
@@ -28,12 +28,12 @@
 model Addres {
   id      Int    @id @default(autoincrement())
   rua String
-  numero Int
+  numero Float
   bairro String
   cidade String
   uf String
-  cep Int
+  cep Float
   user    User   @relation(fields: [user_id], references: [id])
   user_id  Int    
 }
```


